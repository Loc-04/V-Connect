# Frontend (Vite): only variables prefixed with VITE_ are exposed to the browser.
VITE_API_BASE_URL=http://localhost:3000
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
# Optional: override reset password redirect URL. Default is `${window.location.origin}/reset-password`.
VITE_SUPABASE_PASSWORD_RESET_REDIRECT_URL=
